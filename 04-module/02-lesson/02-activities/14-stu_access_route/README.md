# Activity: Access Route

## Instructions:

Start by altering your `items.service.ts` file and adding the getItemByIndex method.

Then inside of `item-detail.component.ts` edit your `ngOnInit` method so that it utilizes itemIndex.