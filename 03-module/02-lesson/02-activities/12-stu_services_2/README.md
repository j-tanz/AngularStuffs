# Activity: Angular Services

## Instructions:

Following the steps shown in class, refactor your code such that tasks.service.ts holds all of your CRUD actions and list.component.ts to use the service's CRUD methods.